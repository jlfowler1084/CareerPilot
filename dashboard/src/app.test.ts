// Placeholder test to satisfy vitest run with 0 failures.
// Remove or replace once real tests are added.
describe('scaffold', () => {
  it('is set up correctly', () => {
    expect(true).toBe(true)
  })
})
